# ── Backend service URLs (Render free-tier) ─────────────────────────────────
# These are used by the interactive architecture warmup on the Home page
# AND by the Dashboard analytics/delivery components.
VITE_PACKING_SERVICE_URL=
VITE_DELIVERY_SERVICE_URL=
VITE_ML_SERVICE_URL=
VITE_KSQLDB_SERVER_URL=
VITE_KSQLDB_STUDIO_URL=

# ── Optional warmup / keepalive tuning ───────────────────────────────────────
# VITE_WARMUP_POLL_INTERVAL_MS=8000   # how often to retry while warming (ms)
# VITE_WARMUP_TIMEOUT_MS=120000       # give up after this many ms
# VITE_KEEPALIVE_MS=900000            # ping live services every 15 min

# only added for the convenience to generate orders from dashboard, not used in the actual application
CONFLUENT_BOOTSTRAP=
CONFLUENT_API_KEY=
CONFLUENT_API_SECRET=
KAFKA_ORDERS_TOPIC=
MONGO_URI=
MONGO_DB_NAME=
MONGO_COLLECTION=